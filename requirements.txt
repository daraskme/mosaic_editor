# 基本 (編集機能のみならこれだけで動作)
Pillow
numpy
opencv-python
tkinterdnd2

# 自動検出 (AnimeCensor + SAM2)
# GPU 環境では torch を CUDA ビルドで入れること:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch
torchvision
transformers>=5.0.0

# イラスト/アニメ絵向け検出 (deepghs/anime_censor_detection)
# onnxruntime は CPU 版を明示 (GPU 版は CUDA 12 前提で torch cu13 と競合し、
# 検出モデルが小さいため CPU で十分高速 ~50ms/枚)
dghs-imgutils
onnxruntime
